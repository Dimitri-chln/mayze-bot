/// <reference types="node" />
import { Writable } from 'stream';
export declare class PacketDecoder extends Writable {
    private packetInfo;
    private buffer;
    constructor(options?: any);
    oncePromise<T extends any>(event: string): Promise<T>;
    _write(chunk: Buffer, encoding: any, callback: any): Promise<any>;
    private decodeHeader;
    private getPayload;
    private decodeHandshake;
    private decodePong;
}
